---
import { cv } from "../config/site";
import RootsSection from "./RootsSection.astro";

const defaultCv = cv.files[cv.defaultLocale];
---

<section id="sobre-mi" class="about-section">
  <div class="about-section__content">
    <header class="about-section__header">
      <p class="about-section__label">Perfil</p>
      <h2 class="about-section__title">Sobre mí</h2>
    </header>

    <div class="about-section__body">
      <div class="about-section__bio">
        <p class="about-section__lead">
          Soy <strong>Angular Frontend Engineer</strong> especializado en Angular moderno,
          TypeScript, RxJS, Signals y NgRx, con experiencia construyendo aplicaciones web
          reales desde la arquitectura frontend hasta producción.
        </p>
        <p class="about-section__text">
          Mi foco principal es la arquitectura mantenible, el estado reactivo, SSR,
          rendimiento, integración con APIs, autenticación, accesibilidad y experiencia de
          usuario. También trabajo con NestJS, GraphQL, PostgreSQL, Docker y AWS, lo que me
          permite entender el producto end-to-end sin diluir mi especialización frontend.
        </p>
      </div>

      <RootsSection />

      <div class="about-section__actions">
        <a href="/cv" class="about-section__cv-link"> Ver currículum en PDF </a>
        <a href={defaultCv.path} download class="about-section__cv-download">
          Descargar
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  .about-section {
    padding-top: var(--spacing-section, 6rem);
    padding-bottom: var(--spacing-section, 6rem);
  }

  .about-section__content {
    max-width: 100%;
  }

  .about-section__bio {
    max-width: 72ch;
  }

  .about-section__header {
    display: flex;
    flex-direction: column;
    gap: 0.375rem;
    margin-bottom: 2rem;
  }

  .about-section__label {
    font-family: var(--font-mono, monospace);
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: #14b8a6;
    margin: 0;
  }

  .about-section__title {
    font-size: clamp(1.5rem, 3vw, 2rem);
    font-weight: 600;
    letter-spacing: -0.03em;
    color: #ededed;
    margin: 0;
  }

  .about-section__body {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
  }

  .about-section__lead {
    font-size: 1.0625rem;
    line-height: 1.75;
    color: #ededed;
    margin: 0;
  }

  .about-section__lead strong {
    font-weight: 500;
  }

  .about-section__text {
    font-size: 0.9375rem;
    line-height: 1.8;
    color: #a1a1aa;
    margin: 0;
  }

  .about-section__actions {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.75rem;
    padding-top: 0.5rem;
  }

  .about-section__cv-link {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 44px;
    padding: 0.625rem 1.25rem;
    border-radius: 6px;
    font-size: 0.875rem;
    font-weight: 500;
    background-color: #ededed;
    color: #0a0a0a;
    text-decoration: none;
    transition: opacity 150ms ease;
  }

  .about-section__cv-link:hover {
    opacity: 0.88;
  }

  .about-section__cv-download {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 44px;
    padding: 0.625rem 1.25rem;
    border-radius: 6px;
    font-size: 0.875rem;
    font-weight: 500;
    font-family: var(--font-mono, monospace);
    color: #a1a1aa;
    border: 1px solid #27272a;
    text-decoration: none;
    transition:
      background-color 150ms ease,
      color 150ms ease,
      border-color 150ms ease;
  }

  .about-section__cv-download:hover {
    background-color: #1a1a1a;
    color: #ededed;
    border-color: #3f3f46;
  }
</style>
